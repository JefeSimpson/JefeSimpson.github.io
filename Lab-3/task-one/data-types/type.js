'use strict';

let name = "Nuradil";

alert( `hello ${1}` ); // answer will be hello 1

alert( `hello ${"name"}` ); // answer will be hello name

alert( `hello ${name}` ); // answer will be hello Nuradil